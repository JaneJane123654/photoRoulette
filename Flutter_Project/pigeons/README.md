# pigeons Placeholder

Store typed Flutter-to-Android host API definitions here.

Use Pigeon for:

- silent delete directory selection and scope metadata
- system delete request coordination when plugin behavior is insufficient
- APK install launch and completion callbacks
- any Android-only capability that cannot be expressed safely with existing Flutter plugins
